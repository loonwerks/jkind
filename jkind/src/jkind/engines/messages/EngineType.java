package jkind.engines.messages;

/** Only the engines used for itineraries */
public enum EngineType {
	SMOOTHING, INTERVAL_GENERALIZATION, IVC_REDUCTION, IVC_REDUCTION_ALL, BMC_BASED_CONSISTENCY_CHECKER, CONSISTENCY_CHECKER;
}
