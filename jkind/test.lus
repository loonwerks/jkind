type Lst = induct {cons (head int) (tail Lst) | nil };

recursive plus_one (l : Lst) returns (res : Lst);
let
  res = if (is_nil l) then nil else (cons (1 + (head l)) (plus_one (tail l)));
tel;

node main() returns();
var
  prop : bool;
let
  prop = (head (cons (2-1) (cons (-3+5) nil))) - 1 = -3;
  --%PROPERTY prop;
tel;