-- Error in node main, support elements were not assigned: [x]

node main(x : int) returns (y : int);
var
ok : bool;
let
y = x + (0 -> pre y);

assert x >= 0;
ok = y >= 0;
--%SUPPORT x;
--%PROPERTY ok;
tel;