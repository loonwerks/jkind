-- if you mark 'x' in the support set:     There is not SUPPORT for prop1
-- if z is in the support elements, the result would be :
-- SUPPORT for prop1:  z

node main(out1: int; out2: int) returns (); 
var
   x: bool;
   y: bool; 
   z: bool;
   prop1:bool;

let
  x = y and z ; 
  y = out2 > 0; 
  z = out1 > 10; 
  assert z;
  prop1 = out1 > 0; 
  --%SUPPORT x;
  --%PROPERTY prop1;

tel;